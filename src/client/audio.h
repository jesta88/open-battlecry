#pragma once

typedef struct config_t config_t;

extern config_t* c_audio_master_volume;
extern config_t* c_audio_music_volume;
extern config_t* c_audio_sfx_volume;
extern config_t* c_audio_voice_volume;
extern config_t* c_audio_ambient_volume;

typedef struct WbAudio WbAudio;
